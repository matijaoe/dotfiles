# Do not copy the mtime
--no-mtime

# Embed thumbnail when it makes sense
--embed-thumbnail

# Save all videos under YouTube directory in your home directory
-o ~/YouTube/%(title)s.%(ext)s

# (Optional but nice) write metadata to file
--add-metadata
