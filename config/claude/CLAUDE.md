# Global Claude Code rules

- NEVER commit automatically without explicit approval.
- ALWAYS use braces for control flow statements (`if`, `else`, `for`, `while`, etc.), even for single-line bodies. The body must go on a new line. Never put the expression on the same line as the condition.
