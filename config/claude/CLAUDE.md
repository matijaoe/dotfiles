# Global Claude Code rules

> This file is symlinked to `~/.claude/CLAUDE.md` and applies to all projects.
> Managed in dotfiles at `config/claude/CLAUDE.md`.

- NEVER commit automatically without explicit approval.
